import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

export const HomePageContainer = styled(Box)({
    display: 'grid',
    justifyItems: 'center',
    alignItems: 'center',
});

export const HomePageTitle = styled(Box)({
    padding: 20,
    textAlign: 'center',
});